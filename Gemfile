# frozen_string_literal: true
source 'https://rubygems.org'

# MiniMagick is a smaller version of RMagick
gem 'minimagick'

# Nokogiri makes working with HTML easy
gem 'nokogiri'

# RTesseract can get text from images
gem 'rtesseract'
